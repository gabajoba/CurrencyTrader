<Page
    x:Class="CurrencyTrader.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CurrencyTrader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:CurrencyTrader.Views"
    xmlns:viewmodels="using:CurrencyTrader.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <!--<Frame>
        <views:CalculatorPageView x:Name="CalculatorPage"/>
    </Frame>-->
    <Page.DataContext>
        <viewmodels:MainViewModel/>
    </Page.DataContext>
    <ContentControl Content="{Binding SelectedPage}" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>

</Page>
